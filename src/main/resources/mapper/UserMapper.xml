<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ccbs.careercoachbookingsystem.mapper.UserMapper">
    
    <resultMap id="UserResultMap" type="com.ccbs.careercoachbookingsystem.entity.UserEntity">
        <id property="id" column="id"/>
        <result property="userId" column="user_id"/>
        <result property="name" column="name"/>
        <result property="email" column="email"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>
    
    <select id="findByUserId" resultMap="UserResultMap">
        SELECT * FROM user
        WHERE user_id = #{userId}
    </select>
    
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user (user_id, name, email, created_at, updated_at)
        VALUES (#{userId}, #{name}, #{email}, NOW(), NOW())
    </insert>
    
</mapper>
